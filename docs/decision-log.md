# Decision Log

## 2026-07-30 — 프로젝트 범위

- 기준 사이트의 공개 서비스 범위를 솔로몬 브랜드로 재구성한다.
- 실시간 웹채팅, 로그인, 마이페이지, 별도 관리자 화면은 초기 범위에서 제외한다.
- 영업지역은 전국이다.
- 후기와 작업사진은 네이버 블로그 및 RSS를 사용한다.

## 2026-07-30 — 디자인

- `신뢰할 수 있는 해결사` 방향을 선택한다.
- 기존 빨강·검정 인지 자산을 현대적인 레드 `#D4362E`와 잉크 `#17191C`로 정리한다.
- 실제 로고 전에는 중앙 `BrandLogo` 컴포넌트의 텍스트 워드마크를 사용한다.

## 2026-07-30 — 기술

- Next.js 호환 vinext 스타터, TypeScript, Node 22를 사용한다.
- 회사·서비스·연락처·브랜드·SEO 데이터를 중앙화한다.
- PC/모바일 반응형, sitemap, robots, canonical, JSON-LD를 출시 필수로 본다.
- 다른 컴퓨터에서는 Git과 `.env.example`, `.nvmrc`, TARS 재진입 프로토콜을 사용한다.

## 2026-08-03 — 패키지 매니저와 개발 포트

- 패키지 매니저는 pnpm 10.15.1로 고정한다.
- 개발 서버 기본 포트는 3002를 사용한다.

## 2026-08-04 — 블로그 RSS 연동

- 네이버 블로그 RSS는 서버에서 읽고 1시간 단위로 재검증한다.
- 외부 RSS 장애나 비정상 응답 시 확인된 대표 게시물을 표시한다.
- RSS 파싱과 서버 통신, 카드 UI를 분리해 이미지 정책 변경의 영향 범위를 제한한다.

## 2026-08-05 — 블로그 이미지 정책

- RSS 본문의 첫 번째 이미지를 작업 카드 대표 이미지로 사용한다.
- `blogthumb.pstatic.net`, `blogpfthumb.phinf.naver.net` 호스트만 허용한다.
- 이미지 URL은 HTTPS 원본 경로로 정규화하고, 유효한 이미지가 없으면 카테고리 플레이스홀더를 유지한다.

## 2026-08-05 — 서비스 상세 콘텐츠

- 서비스별 작업 범위, 현장 확인사항, 제외 범위, 절차, FAQ를 slug 기반 도메인 데이터로 관리한다.
- 상세 페이지는 공통 템플릿을 유지하고 서비스 정책 변경은 도메인 데이터 한 곳에서 처리한다.

## 2026-08-05 — 가격 안내 규칙

- 실제 단가 확정 전에는 금액을 추정하거나 표시하지 않는다.
- 서비스, 규모, 오염도, 접근성, 추가 작업을 평가해 필요한 상담 수준과 비용 영향 요인을 안내한다.

## 2026-08-05 — 지역 페이지

- 전국 영업 범위는 8개 권역 허브로 제공하고 지역명만 바꾼 시·군·구 대량 페이지는 만들지 않는다.
- 권역별 출장 조건과 주요 상담 지역을 고유 콘텐츠로 제공하고 sitemap에 포함한다.

## 2026-08-05 — 견적 서버 검증

- 브라우저 필수값에 의존하지 않고 모든 견적 입력을 서버 도메인 함수에서 검증·정규화한다.
- 저장 adapter 연결 전 검증 endpoint는 개인정보를 보관하지 않는다.

## 2026-08-05 — 견적 사진 저장

- 견적 사진은 공개 URL이 아닌 비공개 R2 key로 저장한다.
- 원본 파일명은 저장 경로에 사용하지 않고 랜덤 UUID와 MIME 기반 확장자를 사용한다.
- 최대 8장, 장당 10MB, JPG·PNG·WebP만 허용하며 저장소 미연결 시 실패를 명시한다.

## 2026-08-05 — 견적 저장

- 정규화된 견적은 D1에 저장하고 사진은 동일한 내부 견적 ID 아래의 비공개 R2 key로 연결한다.
- 고객에게는 내부 UUID 대신 날짜와 임의 문자열로 구성된 접수번호를 반환한다.
- D1 binding이 없으면 실제 접수로 오인되지 않도록 503으로 실패한다.

## 2026-08-05 — 담당자 알림

- 담당자 이메일은 provider interface 뒤의 Resend HTTPS adapter로 발송한다.
- 접수번호를 idempotency key로 사용하고, 알림 실패가 저장된 견적 접수를 롤백하지는 않는다.
- 실제 발송에는 Resend API 키, 검증된 발신주소, 담당자 수신주소가 필요하다.

## 2026-08-05 — 실제 연락처

- 명함과 공식 블로그에서 확인한 전화 `010-5207-1642`, 이메일 `school88@nate.com`, 카카오톡 ID `schoo88`을 중앙 설정으로 관리한다.
- 카카오 상담방 URL이 없는 동안 카카오 버튼을 견적 페이지로 위장 연결하지 않고 이메일 상담을 표시한다.
- 상담방 URL은 `NEXT_PUBLIC_KAKAO_URL` 한 곳에 주입하면 전체 공통 모바일 상담 영역에 반영된다.

## 2026-08-05 — 가격·VAT 공개 계약

- 확인되지 않은 서비스 단가는 계속 공개하지 않고 상담 견적제로 명시한다.
- VAT 포함·별도 여부가 확정되기 전에는 견적서에서 반드시 표시한다는 운영 계약만 안내한다.
- 실제 단가와 VAT 정책은 중앙 `pricePolicy`가 소유하며, 대표 확인 후 한 곳에서 전환한다.

## 2026-08-05 — 회사 증빙 공개

- 명함에서 확인한 대표자와 팩스는 회사 기본정보로 사용한다.
- 사업자등록, 폐기물 허가·신고, 보험, 정량 실적은 증빙이 제공되기 전까지 미확인 상태로 유지한다.
- 실제 작업 근거는 공식 블로그 기록으로 연결하며 확인되지 않은 숫자나 보장 문구는 만들지 않는다.

## 2026-08-05 — 법적 고지 초안

- 개인정보처리방침은 실제 견적 입력과 사진 저장·이메일 알림 흐름을 기준으로 작성한다.
- 보유기간, 운영 위탁사, 예약금과 시점별 취소 비용은 대표 승인 전까지 초안 상태로 명시한다.
- 견적 요청은 예약이나 계약 확정이 아니며 법령상 소비자 권리를 제한하는 환불 문구를 만들지 않는다.

## 2026-08-05 — 반응형 브라우저 QA

- 1440px 데스크톱과 390px 모바일에서 대표 공개 경로를 실제 Chromium으로 점검한다.
- 공개 HTML 경로는 200 응답, 고유 title, H1 1개, 가로 넘침 없음 조건을 적용한다.
- 사이트 URL 미설정 로컬 환경은 `http://localhost:3002`를 사용해 예시 도메인으로 자산 요청이 나가지 않게 한다.

## 2026-08-05 — 구조화 데이터

- LocalBusiness는 검증된 전화·이메일·전국 서비스 범위만 포함하고 확인되지 않은 주소·평점·영업시간은 제외한다.
- 서비스와 지역 스키마는 하나의 `#business` 엔티티를 참조하고 각 canonical URL을 포함한다.
- FAQ 스키마는 화면에 실제로 렌더링되는 질문·답변 도메인 데이터에서만 생성한다.

## 2026-08-05 — 배포 패키지

- Sites의 D1 `DB`와 비공개 R2 `R2` 논리 바인딩을 사용해 견적과 현장 사진을 저장한다.
- 프로덕션 패키지에는 Worker 엔트리, 호스팅 설정, Drizzle 마이그레이션을 포함한다.
- 실제 배포와 도메인 연결은 외부 프로덕션 변경이므로 사용자 승인과 소유 도메인을 받은 뒤 진행한다.
- 소셜 공유 이미지는 확정된 레드·블랙 브랜드와 메인 카피만 사용하고 검증되지 않은 실적을 넣지 않는다.

## 2026-08-05 — 운영 설정 소유권

- 실제 배포와 도메인 연결은 사용자가 Vercel에서 진행하며 Sites 배포는 수행하지 않는다.
- 카카오톡 상담방은 `https://open.kakao.com/o/gRiUs6Lf`로 연결한다.
- 변경 가능성이 큰 연락처, 가격·VAT, 사업 증빙, 개인정보·예약 정책은 `src/config/operations/`에서 각각 분리 관리한다.
- 미확정 값은 빈 문자열, `null`, 빈 배열과 `pending` 상태로 명시해 실제 정보처럼 노출되지 않게 한다.

## 2026-08-05 — 블로그 RSS 전체 표시

- Node Fetch 헤더는 ASCII 전용 User-Agent를 사용해 한글 ByteString 오류를 방지한다.
- 메인페이지는 대표 글 3개만 표시하고 후기·작업사례 페이지는 RSS가 제공하는 전체 항목을 표시한다.
- RSS 장애 시 대표 글 fallback을 유지하되 실패 원인을 서버 로그에 남긴다.

## 2026-08-05 — Vercel 표준 Next.js 전환

- 초기 Sites/vinext 배포 결정을 폐기하고 공식 Next.js 빌드와 Vercel 배포 구조를 사용한다.
- 견적 데이터는 Vercel Marketplace Postgres에 저장하고 현장 사진은 비공개 Vercel Blob에 저장한다.
- 사진은 Vercel Function 요청 크기 제한을 피하기 위해 브라우저에서 Blob으로 직접 업로드한 뒤 검증된 비공개 경로만 견적 API에 전달한다.
- 이 결정은 위의 Sites D1/R2 배포 패키지 결정을 대체한다.

## 2026-08-05 — 작업사례 통합과 메인 비주얼 강화

- 동일한 네이버 작업 기록을 중복 표시하던 작업사례와 블로그 후기 화면을 작업사례로 통합한다.
- 기존 `/reviews` 주소는 `/portfolio`로 영구 이동해 기존 링크를 보존한다.
- 서비스 카드에는 각 서비스를 빠르게 구분할 수 있는 시각 아이콘을 제공한다.

## 2026-08-05 — 서비스 설명 일러스트

- 메인 히어로 그래픽은 기존 디자인을 유지한다.
- 메인 중간에는 솔로몬의 빨강·차콜 작업복을 사용한 고유 일러스트 장면을 배치해 청소·폐기·정리의 통합 서비스와 사진 견적 흐름을 설명한다.
- 기준 사이트는 이미지 활용 밀도만 참고하고 캐릭터, 색상, 구도와 브랜드 요소는 복제하지 않는다.

## 2026-08-07 — 폐기물 카테고리별 비용안내

- 폐기물 비용안내는 통합 페이지와 산업·가정·생활·이사 폐기물 4개 상세 페이지로 구성한다.
- 기준 사이트의 정보 위계만 참고하고 문구, 브랜드 표현과 상담 정책은 솔로몬 기준으로 독립 작성한다.
- 미확정 금액을 경쟁사 수치로 대체하지 않으며 카테고리 단가는 `wasteCategoryRates` 한 곳에서 관리한다.
- 상세 페이지는 고유 canonical 메타데이터, 화면과 동일한 FAQ 구조화 데이터, 사이트맵 주소를 제공한다.
