import { jsonb, pgTable, text, timestamp } from "drizzle-orm/pg-core";
export const estimates = pgTable("estimates", { id: text("id").primaryKey(), reference: text("reference").notNull().unique(), service: text("service").notNull(), customerName: text("customer_name").notNull(), phone: text("phone").notNull(), area: text("area").notNull(), description: text("description").notNull(), preferredDate: text("preferred_date"), photoKeys: jsonb("photo_keys").$type<string[]>().notNull(), status: text("status").notNull().default("received"), createdAt: timestamp("created_at", { withTimezone: true }).notNull() });
