import { sqliteTable, text } from "drizzle-orm/sqlite-core";
export const estimates = sqliteTable("estimates", { id: text("id").primaryKey(), reference: text("reference").notNull().unique(), service: text("service").notNull(), customerName: text("customer_name").notNull(), phone: text("phone").notNull(), area: text("area").notNull(), description: text("description").notNull(), preferredDate: text("preferred_date"), photoKeys: text("photo_keys", { mode: "json" }).$type<string[]>().notNull(), status: text("status").notNull().default("received"), createdAt: text("created_at").notNull() });
